require 'test_helper'

class PostfixAliasTest < ActiveSupport::TestCase
  test "create an alias" do
    #a = PostfixAlias.new()
    #a.from="blah@obsqure.me"
    #a.to="test@test.com"
    #assert a.save
  end

  # don't allow non-unique from
end
