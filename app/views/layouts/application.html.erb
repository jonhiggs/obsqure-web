<!DOCTYPE html>
<html>
<head>
  <title>
    <%= 
      # FIXME: this is super-hacky
      @page = "Register" if params["controller"] == "devise/registrations" && params["action"] == "new"
      @page = "Update Details" if params["controller"] == "devise/registrations" && params["action"] == "edit"
      @page = "Sign In" if params["controller"] == "devise/sessions"
      [ "Obsqure", @page ].compact.flatten.join(" - ")
    %>
  </title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "yui-min.js" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div id="main">
  <%= render :partial => "shared/login" unless user_signed_in? || params["controller"] == "devise/sessions" %>
  <%= render :partial => "shared/links" %>
  <%= render :partial => "layouts/messages" %>
  <div id="content">
    <%= yield %>
  </div>
  <%= render :partial => "shared/footer" %>
</div>

</body>
</html>
